const NoData = () => {
    return (
      <div className="no_data">
        <img src="/assets/images/no_data.svg" alt="Não encontrado" />
        <span>Ops, parece que não encontramos o que você está buscando. Que tal tentarmos de novo?</span>
      </div>
    );
}

export default NoData;